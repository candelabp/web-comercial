---
import Layout from '../layouts/Layout.astro';

const whatsappLink = 'https://wa.me/2634692670';
---

<Layout title="Contacto">
  <main class="container contact-page">
    <section class="contact-panel">
      <p class="eyebrow">CONTACTO</p>
      <h1>Hablemos de tu próximo sitio web</h1>
      <p>
        Si querés mejorar tu presencia digital, escribime y lo vemos juntas.
        Te respondo con una propuesta clara según tus objetivos.
      </p>

      <div class="contact-actions">
        <a href={whatsappLink} target="_blank" rel="noopener noreferrer" class="btn btn--primary">
          Escribirme por WhatsApp
        </a>
        <a href="mailto:candepuerta@gmail.com" class="btn btn--ghost">
          Enviar email
        </a>
      </div>
    </section>
  </main>
</Layout>

<style>
.contact-page {
  max-width: 860px;
  padding-top: 3.8rem;
  padding-bottom: 4.5rem;
}

.contact-panel {
  background: #fff;
  border: 1px solid var(--line);
  border-radius: var(--radius-lg);
  box-shadow: none;
  padding: 2rem;
}

.eyebrow {
  display: inline-block;
  margin-bottom: 0.9rem;
  font-size: 0.75rem;
  letter-spacing: 0.14em;
  font-weight: 800;
  color: #854d0e;
  background: var(--primary-soft);
  border: 1px solid #fde68a;
  border-radius: 999px;
  padding: 0.35rem 0.75rem;
}

h1 {
  font-size: clamp(2rem, 4vw, 2.7rem);
  margin-bottom: 0.9rem;
}

.contact-panel p {
  color: var(--muted-color);
  font-size: 1.05rem;
}

.contact-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 0.75rem;
  margin-top: 1.4rem;
}

.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  text-decoration: none;
  font-weight: 700;
  border-radius: 12px;
  padding: 0.75rem 1rem;
}

.btn--primary {
  color: #111827;
  background: var(--primary-color);
}

.btn--ghost {
  border: 1px solid var(--line);
  background: #fff;
}

@media (max-width: 768px) {
  .contact-page {
    padding-top: 2.7rem;
  }
}
</style>
